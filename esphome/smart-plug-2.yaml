esphome:
  name: smart-plug-2
  friendly_name: Smart Plug 2
  on_boot:
    - lambda: |-
        id(my_ota).set_auth_password("");

esp8266:
  board: esp01_1m

# Enable Home Assistant API
api:

ota:
  - platform: esphome
    password: "4461175472ba29414801ce544f655737"
    id: my_ota

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  manual_ip:
    static_ip: 192.168.0.135
    gateway: 192.168.0.1
    subnet: 255.255.255.0

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Smart-Plug-1 Fallback Hotspot"
    password: "pWNqb8sWqTie"

captive_portal:

packages:
  device_base: !include smart-plug.base.yaml